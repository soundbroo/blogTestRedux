const initialState = {
    id: 0,
    post: [],
    user: [],
    comments: []
}

export const post = (state = initialState, action) => {
    switch (action.type) {
        default: return state
    }
}